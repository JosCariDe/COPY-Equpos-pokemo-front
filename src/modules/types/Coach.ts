export interface Entrenador {
    id: number;
    fisrtName: string;
    lastName: string;
    email: string;
    birthDate: Date;
}

export interface Trainer {
    id: number;
    fisrtName: string;
    lastName: string;
    email: string;
    birthDate: Date;
}